import json
import math

import torch
import torch.nn as nn
import torch.optim as optim
from nfnets import AGC

def create_lr_scheduler(optimizer,
                        num_step: int,
                        epochs: int,
                        warmup=True,
                        warmup_epochs=1,
                        warmup_factor=1e-3,
                        end_factor=1e-6):
    assert num_step > 0 and epochs > 0
    if warmup is False:
        warmup_epochs = 0

    def f(x):
        """
        根据step数返回一个学习率倍率因子，
        注意在训练开始之前，pytorch会提前调用一次lr_scheduler.step()方法
        """
        if warmup is True and x <= (warmup_epochs * num_step):
            alpha = float(x) / (warmup_epochs * num_step)
            # warmup过程中lr倍率因子从warmup_factor -> 1
            return warmup_factor * (1 - alpha) + alpha
        else:
            current_step = (x - warmup_epochs * num_step)
            cosine_steps = (epochs - warmup_epochs) * num_step
            # warmup后lr倍率因子从1 -> end_factor
            return ((1 + math.cos(current_step * math.pi / cosine_steps)) / 2) * (1 - end_factor) + end_factor

    return torch.optim.lr_scheduler.LambdaLR(optimizer, lr_lambda=f)

def get_params_groups(model: torch.nn.Module, weight_decay: float = 1e-5):
    # 记录optimize要训练的权重参数
    parameter_group_vars = {"decay": {"params": [], "weight_decay": weight_decay},
                            "no_decay": {"params": [], "weight_decay": 0.}}

    # 记录对应的权重名称
    parameter_group_names = {"decay": {"params": [], "weight_decay": weight_decay},
                             "no_decay": {"params": [], "weight_decay": 0.}}

    for name, param in model.named_parameters():
        if not param.requires_grad:
            continue  # frozen weights

        if len(param.shape) == 1 or name.endswith(".bias"):
            group_name = "no_decay"
        else:
            group_name = "decay"

        parameter_group_vars[group_name]["params"].append(param)
        parameter_group_names[group_name]["params"].append(name)

    # print("Param groups = %s" % json.dumps(parameter_group_names, indent=2))
    return list(parameter_group_vars.values())

def get_optims_and_scheduler(model, cfg, steps_per_epoch):
    pg = get_params_groups(model, weight_decay=cfg.visual_encoder_params.wd)
    if cfg.visual_encoder_params.optimizer == "adam":
        optimizer = optim.AdamW(pg, lr=cfg.visual_encoder_params.lr, weight_decay=cfg.visual_encoder_params.wd)

    elif cfg.visual_encoder_params.optimizer == "sgd":
        optimizer = optim.SGD(pg, lr=cfg.visual_encoder_params.lr, weight_decay=cfg.visual_encoder_params.wd, momentum=0.9)
    lr_scheduler = create_lr_scheduler(optimizer, steps_per_epoch, cfg.visual_encoder_params.visual_n_epoch,
                                       warmup=True, warmup_epochs=1)
    return optimizer, lr_scheduler

